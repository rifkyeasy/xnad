{
  "name": "@xnad/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Smart contracts for xnad trading agent",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:gas": "forge test --gas-report",
    "clean": "forge clean",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast",
    "deploy:monad": "forge script script/Deploy.s.sol --rpc-url $RPC_URL --broadcast --verify"
  },
  "devDependencies": {}
}
